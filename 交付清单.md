# Luvvv-agent 交付清单（worker 层升级版）

## 镜像产物

| 项 | 值 |
|---|---|
| 镜像名 | `luvvv-managed:latest` |
| 打包文件 | **`luvvv-managed-final.tar.gz`**（1.3G gzip，**含 root 修复**） |
| 架构 | **amd64 / linux**（平台 x86_64 匹配） |
| 解压后大小 | 3.25GB |
| 运行用户 | **ctf（非 root）**——关键修复 |

## ⚠️ 关键修复（v2）：claude 非 root 运行

**线上发现**：所有 explore 报 `rc=1; conclude failed`。根因是镜像以 **root** 运行，而 Claude Code 2.1.227 出于安全**拒绝 root 下 `--dangerously-skip-permissions`**（claude 进程直接 rc=1 退出），explore 无法让 claude 自主跑 bash。

**修复**：Dockerfile 新增非 root 用户 `ctf`（`useradd` + `chown /app` + `USER ctf`），整个 orchestrator/solver 以 ctf 运行，claude 会话落到 `/home/ctf/.claude`。已实测：镜像内 ctf + skip-permissions + bash 工具可正常执行（exit 0）。

**附带**：SessionWorker 错误信息现在带 claude stderr 片段，`rc=1` 不再盲猜。

## 镜像内关键组件（已验证）

- **claude CLI 2.1.227** + **Node v22.13.0**（SessionWorker 驱动，cairn 式 session+conclude）
- 工具链：nuclei / httpx / dalfox / cloudfox / chisel / ffuf / gobuster / wfuzz / dirb / sqlmap / nmap / hydra / john / hashcat / proxychains4 / gdb / binutils / pwntools / impacket / capstone / z3
- 知识库：`/opt/PayloadsAllTheThings`（452 文件）+ `/opt/InternalAllTheThings`（184 文件）+ `/opt/tools.md`
- 环境变量：平台注入 `ANTHROPIC_BASE_URL/AUTH_TOKEN/MODEL` + `BENCHMARK_TOKEN/BASE_URL` 后 entrypoint 自动开跑

## 核心代码改动（本轮）

| 文件 | 改动 |
|---|---|
| `solver/worker.py` | **SessionWorker**（`claude --session-id` 执行 + 超时/失败 `-r` conclude 兜底 + 结构化返回）；已删 HermesWorker 死代码 |
| `solver/contracts.py` | 新增：严格 JSON 契约（accepted 包裹 + 各 phase 形状校验） |
| `solver/prompts/*.md` | 反漂移（先读题面/工具优先/keep-working/证据纪律/增量事实）+ conclude 模板 |
| `solver/loop.py` | 三阶段接线 SessionWorker + evidence 证据优先上报（flag 只信命令输出） |
| `orchestrator/scheduler.py` | 进度式回收（无新 fact/flag 提前关容器换题，治 3 槽卡死） |
| `config.json` | worker=claude + 四档超时 + scheduler 回收参数 |
| `Dockerfile.managed` | 去 hermes、装 claude CLI（Node 22）、build-assets COPY 提取 |
| `adapters/mock.py` + `scripts/mock_bench.py` | 本地回归用（3 题 mock，不进镜像生产路径） |

## 本地回归跑法

```bash
python3 scripts/mock_bench.py &            # 起 mock 平台 + 3 靶场
python3 -m orchestrator.main --adapter mock --no-precheck --no-server --auto-exit
```

实测：3/3 全解出，每题 ~20-27s（bootstrap 阶段解决），~30s 完成 3 题并行。

## 备注

- `build-assets/`（179MB）：构建资源（宿主机 7890 预下载），**不进镜像**；重建需保留，交完平台可删
- 平台提交代码未动（`adapters/tsec.py` 保持原样）
